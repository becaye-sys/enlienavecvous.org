{% extends "email_layout.html.twig" %}

{% block body %}
    <p>Bonjour,</p>
    <p>Nous sommes désolé.e.s pour cet inconvénient, mais votre rendez-vous a été annulé par votre thérapeute :</p>

    <p>{{ appointment.cancelMessage }}</p>

    <p>Le {{ appointment.bookingDate|date('d/m/Y') }}</p>
    <p>À {{ appointment.bookingStart|date('H:i') }}</p>
    {% if appointment.therapist.displayName is not null %}
        <p>Avec {{ appointment.therapist.displayName }}</p>
    {% else %}
        <p>
            Avec {{ appointment.therapist.firstName }} {{ appointment.therapist.lastName }}
        </p>
    {% endif %}
    <p>Tel: {{ appointment.therapist.phoneNumber|phone_prefix(appointment.therapist.country, appointment.therapist.phoneNumber) }}, email: {{ appointment.therapist.email }}</p>

    <p>Nous vous invitons à <a href="{{ project_url }}">reprendre rendez-vous</a> dès maintenant avec ce thérapeute ou un.e autre thérapeute via la
        plateforme.</p>

    <p>Cordialement,</p>
    <p>L'équipe de Enlienavecvous.org</p>
{% endblock %}